# Compose Director — Golden Key Brand / Company

Use the `edit_decisions.render_runtime` approved in `proposal_packet`; pass the proposal to
OpenMontage `video_compose` so runtime-swap detection remains active. Never silently replace
Remotion, HyperFrames, FFmpeg, provider, voice, music, or identity treatment.

Render only asset-manifest inputs and mix approved speech, brand music motif, natural sound,
and SFX. Verify existence, ffprobe, full decode, duration, resolution, frame rate, audio,
wrapping/safe zones, logo/text/color/person/place/product preservation, company-evidence
adjacency, brand distinctness, and generated disclosure.

Write schema-valid `render_report` and `final_review`. Inspect approximately
`duration-1.0s`, `duration-0.5s`, and `duration-0.1s`; listen through the tail and record
`final_review.checks.ending_closure`. The brand line/memory device must be readable, the visual
release visible, and audio untruncated. A technically valid generic or accidental ending fails.
